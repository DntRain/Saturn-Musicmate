name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: windows-latest
            args: ""
          - platform: macos-latest
            args: "--target universal-apple-darwin"
          - platform: ubuntu-22.04
            args: ""

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "./src-tauri -> target"

      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev \
            libsoup-3.0-dev \
            libjavascriptcoregtk-4.1-dev \
            libasound2-dev \
            libpulse-dev \
            libjack-jackd2-dev

      - name: Install frontend dependencies
        run: npm ci

      - name: Bundle QQ Music service for Windows
        if: matrix.platform == 'windows-latest'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force src-tauri/resources | Out-Null
          git clone --depth 1 https://github.com/Rain120/qq-music-api.git src-tauri/resources/qq-music-api
          npm install --prefix src-tauri/resources/qq-music-api --no-audit --no-fund --loglevel=error
          New-Item -ItemType Directory -Force src-tauri/resources/node | Out-Null
          Copy-Item (Get-Command node).Source src-tauri/resources/node/node.exe

      - name: Build and release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "Musicmate ${{ github.ref_name }}"
          releaseBody: |
            Musicmate ${{ github.ref_name }}

            Download the installer for your platform below.

            - **Windows**: `.msi` or `.exe` (NSIS)
            - **macOS**: `.dmg` (universal: Intel + Apple Silicon)
            - **Linux**: `.AppImage` or `.deb`

            > Windows installers include the local QQ Music sidecar service. Fill in QQ Music cookies in Settings before using online search or VIP preview.
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}
